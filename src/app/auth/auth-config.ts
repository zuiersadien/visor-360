// app/auth/auth-config.ts
export const authConfig = {
  cookiePrefix: "myapp", // Cambia "myapp" por el prefijo que quieras para tus cookies
  // Puedes agregar otras configuraciones según necesites
}
